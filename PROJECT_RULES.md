# LinH Pocket 项目规则

## 一、项目与架构
- 项目名称：LinH Pocket。
- 当前技术架构：Next.js 框架（不要把它改成纯 HTML 单页，不要乱删 middleware 和 API 后端，保持现有架构）。
- 产品形态：模拟手机 App，采用 Vercel 部署，必须支持 PWA 体验。

## 二、视觉与交互美学（最高准则）
- 风格：Apple 极简克制，黑白韩系 ins 风，iOS 玻璃拟态。
- 色彩：背景深空灰 #1c1c1e 或纯黑 #000000；文字纯白 #ffffff；次级文字浅灰 #8e8e93；禁止纯蓝纯灰大色块。
- 毛玻璃：卡片、弹窗必须用 backdrop-filter: blur(20px) saturate(180%)，背景半透明，极细发丝边框。
- 动效：必须有物理弹性（cubic-bezier(0.175, 0.885, 0.32, 1.275)），点击缩放 0.98，拒绝生硬。
- 适配：必须考虑 safe-area-inset（刘海屏、灵动岛、底部横条），使用 100dvh，滑动区域必须有 -webkit-overflow-scrolling: touch。

## 三、开屏动画、页面行为与导航规则
1. 开屏动画：每次打开 App 首先播放专属开屏动画。开屏动画必须为纯黑底 + 中心玻璃质感 Logo 呼吸发光动画（黑白玻璃拟态风格，可用发丝线光环展开、品牌名淡入），动画结束后自动平滑过渡，不需要用户点击。
2. 导航流程：开屏动画结束后进入锁屏页；锁屏页为仿 iOS 极简风格（超大纤细时钟、日期），必须支持向上滑动解锁；解锁后平滑过渡进入主界面（主页）。完整链路固定为：开屏动画 → 锁屏页 → 向上滑动解锁 → 主页。
3. 返回键：当前项目里的返回键（纯文字、无图标）已经做好了，效果很好。不要修改它，保留现状。
4. 更新弹窗规则：每次打开 App 时，检查 localStorage 中记录的已读版本；存在新版本（版本号比已读记录新，包含新补丁）时必须弹出更新日志弹窗，展示最近的更新点。新用户、老用户都必须弹，不区分新老用户。用户点击"知道了"后，把当前版本写入 localStorage 作为已读记录，弹窗关闭；下次发版（新版本/新补丁）时再次弹出。

## 四、更新日志与颜色规范（重要，不能删）
- 更新日志颜色标记必须严格按以下四色执行（含色值）：
  - 新增 = 绿色 (#34c759)
  - 修复 = 红色 (#ff3b30)
  - 调整 = 蓝色 (#0a84ff)
  - 补丁 = 紫色 (#bf5af2)
- 备注：所有彩色标签仅允许出现在更新日志面板和更新通知弹窗中，其余界面保持黑白拟态，禁止出现上述彩色。
- 每次更新，所有用户（新老）必须看到通知，不区分新老用户。
- 通知方式：白色拟态小圆点（设置图标角标 + 更新日志行内点）。点击圆点弹出更新日志弹窗，展示完整更新日志。已读记录存 localStorage，有新版/新补丁时圆点重新出现。

## 五、自动推送与部署规则
- 当我向你发送指令，比如"上线"、"推送"、"自动部署"，或者我在完成一段代码修改后告诉你"好了"，请你主动帮我在终端执行以下命令：
  `git add .`
  `git commit -m "update: 自动推送"`
  `git push origin main`
- 推送前，必须确认没有把敏感文件（.env、密钥、API Key）推上去。
- 如果 push 因为网络报错（比如 Failed to connect to github.com port 443），请自动重试一次。

## 版本号细分规范（核心版本与补丁版本）
1. 【常规升级】：如果本次更新包含“新增”或“调整”（例如增加新功能、大范围重构、整体UI大改），版本号按主版本号递增（例如 0.1 -> 0.2 -> 0.3）。
2. 【补丁升级】：如果本次更新**只有“修复”或“补丁”**（只修改了颜色、修复了白屏、微调了细节），版本号按小版本号递增（例如 0.2 -> 0.2.1 -> 0.2.2，以此类推）。
3. 【用户指定】如果用户明确说了版本号，则完全听从用户指令。

## UI 美化与修改自由度（放宽限制）
1. 【大胆美化】：在不破坏现有 HTML 结构和项目类名（`class` 名称）的前提下，你必须主动、大胆地对主界面、设置页、按钮和弹窗进行视觉美化。不要因为担心出错而不敢改样式（CSS）！
2. 【安全底线】：绝对不允许删除或重写现有的核心 `class` 类名（因为这会破坏全局样式系统）。绝对不允许碰 `middleware.ts`、`app/api/*` 等后端逻辑。
3. 【美学要求】：大胆尝试高质感的毛玻璃（Backdrop-filter）、精致的微光阴影（Box-shadow）、弹性按压反馈（Active:scale）和高级排版（字重/字间距）。遇到丑的设计，直接按你的审美直觉去重构 UI！
4. 【渐进式安全提交】：每次修改完一个独立模块（如设置页、主屏幕、Dock栏），就自动执行一次推送。这样即便某个地方出了小 Bug，也方便快速回滚，不会因为一次大重构导致整个项目崩溃。
